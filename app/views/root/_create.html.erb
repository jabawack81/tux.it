<%= render :partial => 'form'%>
<div id="answer">
<h1><%= @showurl.address%></h1>

<p>is now accessible via: <input type="url" value="http://<%= request.host %>/<%=  @showurl.minified %>" size="15" /></p>

<p><%= image_tag("http://#{request.host}/#{@showurl.minified}.qr")  %> code for this QR tag:
  <textarea><%= image_tag("http://#{request.host}/#{@showurl.minified}.qr") %></textarea>
</p>

<p>link stats <a href="<%= info_path(@showurl.minified)%>"> here</a></p>
</div>
