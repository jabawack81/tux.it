<%= render :partial => 'form'%>
<h1><%= @entry.address %></h1>
<div id="stats">
  <ul>
  <li><%= image_tag(@chart)  %></li>
  <% @entry.views.each do |view|  %>
  <li> <%= view.created_at %> / <%= view.referrer %> / <%= view.user_agent %> </li>
  <% end %>
</ul>
</div>
